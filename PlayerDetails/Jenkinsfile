pipeline {

    stages {
        stage('Example Build') {
            steps {
                
                echo 'Hello, Maven'
                sh 'mvn clean install'
            }
        }
        
    }
}
