pipeline {
    agent none 
    stages {
        stage('Example Build') {
            steps {
                agent none
                echo 'Hello, Maven'
                sh 'mvn clean install'
            }
        }
        
    }
}
